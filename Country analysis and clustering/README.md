# Анализ и кластеризация стран методом K-Means.
Проведем группировку стран с помощью K-Means++	(метод локтя)

# Цель: 
найти похожие между собой страны и регионы, экспериментируя с количеством кластеров и выяснит, что означают эти кластеры.
- Построили график с странами, где население меньше 0,5 миллиарда людей.
- Построили график с средим ВВП на душу населения (GDP per Capita) для каждого региона.
- Построили график, показывающий связь между колонками с количеством телефонов на тысячу человек и ВВП на душу населения.
- Посмотрили на связь между ВВП на душу населения и грамотностью (Literacy).
- Построили тепловую карту для корреляции между колонками.
- Построили график иерархической кластеризации данных.
- Мы выполнили группировку данных по колонке region (df.groupby('region')), далее взяли только колонку Climate, преобразовали при помощи усреднения mean (transform('mean')) и передали информацию в метод .fillna, чтобы заменить отсутствующие значения Nan.
- Создали и оучили несколько моделей при помощи цикла for (метод "локтя", проверили количество кластеров, сохранили сумму квадратов расстояний от точек до центра для каждой модели в цикле, нанесли данные на график и изобразили на нем метрику для различных значений К).
- Нанесли страны на географическую карту

Данные содержат следующую информацию по странам:
 -   Country 
 -   Region 
 -   Population 
 -   Area (sq. mi.)  
 -   Pop. Density (per sq. mi.) 
 -   Coastline (coast/area ratio) 
 -   Net migration 
 -   Infant mortality (per 1000 births)
 -   GDP ($ per capita)
 -   Literacy (%)
 -  Phones (per 1000)
 -  Arable (%)
 -  Crops (%)
 -  Other (%)
 -  Climate
 -  Birthrate
 -  Deathrate
 -  Agriculture
 -  Industry
 -  Service
```
# Стек / основные иснтрументы
import pandas as pd

import numpy as np

import seaborn as sns

import matplotlib.pyplot as plt

from PIL import Image

import plotly.express as px

# ML инструменты
from sklearn.preprocessing import StandardScaler

from sklearn.cluster import KMeans
